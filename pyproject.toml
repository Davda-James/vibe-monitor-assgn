[project]
name = "vibe-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.116.1",
    "prometheus-fastapi-instrumentator>=7.1.0",
    "requests>=2.32.5",
    "uvicorn>=0.35.0",
]
